language: cpp
compiler:
  - gcc
script:
    mkdir build && cd build
    cmake ..
    make
    make test
branches:
  only:
    - master
notifications:
  recipients:
    - virako.9@gmail.com
  email:
    on_success: change
    on_failure: always
before_install:
  - sudo apt-cache search boost | grep test
  - sudo apt-get install cmake libboost # irrlicht
